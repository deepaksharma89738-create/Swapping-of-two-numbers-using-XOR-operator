# Swapping-of-two-numbers-using-XOR-operator
Program swaps two numbers using the XOR operator without using a third variable. First, the user enters two numbers. The program applies XOR operations step by step: first number = first XOR second, second = first XOR second, and first = first XOR second. After these operations, the values are exchanged. Finally, it displays the swapped numbers.
